# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes. Aliases can be placed here, though oh-my-zsh
# users are encouraged to define aliases within the ZSH_CUSTOM folder.
# For a full list of active aliases, run `alias`.

# copy or move this file to $ZSH_CUSTOM/

# artisan global alias
alias art="artisan"
alias arttest="artisan test"
alias arttinker="artisan tinker"
alias artopt="artisan optimize"

# get machine's ip address
alias ip="ipconfig getifaddr en0"

# edit global zsh configuration
alias zshconfig="vim ~/.zshrc"
# edit profile zsh configuration
alias zshprofile="vim ~/.zprofile"
# edit zshenv configuration
alias zshenv="vim ~/.zshenv"
# reload zsh configuration
alias zshsource="source ~/.zshrc"
# reload zsh configuration
alias ohmyzsh="cd ~/.oh-my-zsh"

# navigate to global ssh directory
alias sshhome="cd ~/.ssh"
# edit global ssh configuration
alias sshconfig="vim ~/.ssh/config"